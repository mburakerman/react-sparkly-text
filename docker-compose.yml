version: "3.4"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frontend
    ports:
      - 3000:3000
    command: yarn dev
    volumes:
      - .:/app
    tty: true
